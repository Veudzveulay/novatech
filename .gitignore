node_modules/
dist/
build/
.DS_Store

# ── Secrets — ajouté au J2 ───────────────────────────────────────────────────
# ATTENTION : .env est DÉJÀ suivi par Git depuis le commit 24b295b. Cette ligne
# empêche de nouveaux ajouts mais ne retire pas le fichier de l'historique.
# La purge relève de la remédiation (cf. docs/blocages-inter-livrables.md § B-02).
.env
.env.*
!.env.example

# ── Artefacts de test (L2) ───────────────────────────────────────────────────
coverage/
playwright-report/
test-results/
.e2e-uploads/
npm-audit.json
npm-audit.html
gitleaks-report.sarif
trivy-fs.sarif
zap-report/
